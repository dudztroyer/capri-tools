import { CSSProperties } from "react";

export interface AppStyles {
  container: CSSProperties;
  divider: CSSProperties;
  header: CSSProperties;
  headerTitleWrapper: CSSProperties;
  hideTabletDisplayMobile: CSSProperties;
  layout: CSSProperties;
  logoWrapper: CSSProperties;
  navbarContainer: CSSProperties;
  navbarMobile: CSSProperties;
  navMenu: CSSProperties;
  paragraph: CSSProperties;
  placeholder: CSSProperties;
  profileAvatar: CSSProperties;
  profileMenu: CSSProperties;
  section: CSSProperties;
  sider: CSSProperties;
  siderContent: CSSProperties;
  title: CSSProperties;
}

